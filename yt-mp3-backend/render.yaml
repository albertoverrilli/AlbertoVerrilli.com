services:
  - type: web
    name: yt-mp3-api
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        # Replace with your actual site domain (comma-separated for multiple)
        value: https://www.albertoverrilli.com,https://albertoverrilli.com
